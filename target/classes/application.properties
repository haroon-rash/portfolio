# ----------------------------
# Application Name
# ----------------------------
spring.application.name=portfolio

# ----------------------------
# Security Configuration
# ----------------------------
# Admin credentials for login
# These will be set as environment variables on Render
app.admin.username=${APP_ADMIN_USERNAME:HaroonUrRasheed}
app.admin.password=${APP_ADMIN_PASSWORD:Haroon12}

# ----------------------------
# Database Configuration
# ----------------------------
# JDBC URL, username, and password from Neon PostgreSQL
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-super-mountain-ahifr647-pooler.c-3.us-east-1.aws.neon.tech:5432/portfolio?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_QJuXeYnf6T8g}
spring.datasource.driver-class-name=org.postgresql.Driver

# ----------------------------
# JPA / Hibernate
# ----------------------------
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ----------------------------
# Thymeleaf Configuration
# ----------------------------
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML

# ----------------------------
# Error Handling
# ----------------------------
server.error.include-message=always
server.error.include-stacktrace=always

# ----------------------------
# Server Configuration
# ----------------------------
# Use PORT from environment (Render sets it automatically), default 4000 for local
server.port=${PORT:4000}
